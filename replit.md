# Overview

Telegram бот для управління чатом з системою ролей (Owner, Head Admins, Gnomes) та широким функціоналом модерації, пересилання повідомлень, нотаток та днів народження. Бот підтримує логування всіх дій в окремий канал та має можливість працювати з користувачами через ID, username або reply на повідомлення.

**Власник бота:** Santa (@dont_luck, ID: 7247114478)

## Version Control
- **Version 2** (25.11.2025 - 16:38) - Stable checkpoint з UI покращеннями (клікабельні імена, копіюються ID)
- **Version 3** - Working directory для нових змін (з version3_bot.py та version3_database.py)
- **Current Working** - bot.py + database.py (не зберігаємо в checkpoints автоматично)

# User Preferences

Preferred communication style: Simple, everyday language (Ukrainian).

# System Architecture

## Core Bot Framework
- **Technology**: Python with python-telegram-bot library
- **Database**: SQLite для зберігання всіх даних
- **Configuration**: config.json для налаштувань чатів та власників
- **Main Components**: 
  - Система ролей з перевіркою прав доступу
  - 30+ команд для управління та модерації
  - Автоматичне логування дій в канал
  - Онлайн-режими пересилання (sayon/sayson)

## Role-Based Access Control
- **Owner (2 особи: 7247114478, 7516733683)**: Повний доступ до всіх команд, включаючи управління Head Admins та чорний список
- **Head Admins**: Доступ до більшості команд, управління гномами, модерація, нотатки, дні народження
- **Gnomes**: Базові команди пересилання (/say, /says), онлайн-режими, нотатки, нагадування
- **Permission Hierarchy**: Owner > Head Admin > Gnome

**Примітка:** Тільки 7247114478 (Santa) та 7516733683 можуть використовувати команди власника. Решта користувачів мають доступ відповідно до своєї ролі.

## Database Structure (SQLite)
- **Tables**:
  - roles - ролі користувачів (head_admin, gnome)
  - message_mapping - мапінг повідомлень між чатами
  - online_modes - активні режими sayon/sayson
  - bans, mutes - модерація користувачів
  - blacklist - чорний список (тільки Owner)
  - forwarding_stats - статистика пересилань
  - notes - нотатки користувачів
  - reminders - нагадування
  - birthdays - дні народження користувачів
  - birthday_settings - налаштування привітань (GIF + текст)
  - users - інформація про користувачів
  - say_blocks - блокування команд /say
  - action_logs - логи всіх дій

## Message Forwarding System
- **Two Chat System**: Admin chat (адміністрація) та User chat (користувачі)
- **Forwarding Commands**: 
  - `/say` - з підписом автора
  - `/says` - анонімно
  - `/sayon` - автоматичне пересилання з підписом
  - `/sayson` - автоматичне анонімне пересилання
  - `/saypin` - відправити та закріпити
  - `/save_s` - тихе збереження в адмін-чат
- **Reply Support**: Всі команди працюють з reply, ID та username
- **Media Support**: Пересилання фото, відео, документів, стікерів та іншого медіа

## Moderation System
- **Ban Commands**: 
  - `/ban_s` (тихий бан)
  - `/ban_t` (публічний бан) - Динамічне повідомлення, причина необов'язкова
- **Unban Commands**: 
  - `/unban_s` (тихе розблокування)
  - `/unban_t` (публічне розблокування) - Клікабельне імʼя адміна в повідомленні
- **Mute Commands**: 
  - `/mute_s` (тихий мут)
  - `/mute_t` (публічний мут з часовим таймером!) - Формат: `/mute_t [час] [причина]`
    - **Часові формати**: 30s, 1m, 5m (секунди/хвилини), 1h, 2h (години)
    - **Автоматичний розмут**: Через вказаний час бот автоматично розмутить користувача! ⏱️
    - **Динамічне повідомлення**: Показує часові пояси за Київським часом, причина необов'язкова
    - **Приклади**: `/mute_t 1m спам`, `/mute_t 30s`, `/mute_t 2h флуд`
- **Unmute Commands**: 
  - `/unmute_s` (тихе розмут)
  - `/unmute_t` (публічне розмут) - Клікабельне імʼя адміна в повідомленні
- **Other**: 
  - `/kick` (вигнати без бану) - Підтримує причину, клікабельне імʼя адміна
  - `/nah` (чорний список - тільки Owner)
- **Message Format**: Всі публічні повідомлення містять:
  - 🎯 Дію (🚫 бан, 🔇 мут, 🔊 розмут, ✅ розблокування, 👟 вигнання)
  - Часовий статус (час розмута / ∞ для бану)
  - Причину (якщо вказана)
  - Клікабельне імʼя адміна (HTML посилання на профіль)

## Notes and Reminders System
- **Notes**: 
  - `/note` - збереження нотатки
  - `/notes` - перегляд всіх нотаток з номерами
  - `/delnote [номер]` - видалення нотатки за номером (наприклад: `/delnote 1`)
- **Storage**: Автоматичне дублювання в канал нотаток (-1002477496414)
- **Reminders**: `/reminder` для себе, `/reminde` для іншого користувача
- **Time Format**: 1m (хвилини), 1h (години), 1d (дні)

## Birthday System
- **Commands**: `/birthdays` (список), `/addb` (додати)
- **Settings**: `/setbgif` (GIF привітання), `/setbtext` (текст привітання)
- **Preview**: `/previewb` - чистий перегляд привітання (GIF + текст як буде в день народження)
  - Закріплює повідомлення в чаті
  - Додає текст "Давайте привітаємо @username" (або ім'я якщо немає username)
  - Позначає власника для попереднього перегляду
- **Auto-greetings**: Автоматичні привітання щодня о 08:00 AM Київський час
- **Greeting Format**: 
  - Відправляється в USER_CHAT_ID
  - Включає GIF анімацію + текст привітання
  - Додає "Давайте привітаємо @username" (або ім'я)
  - Закріплює повідомлення для видимості
- **Tagging System**: Використовує @username або ім'я користувача для особистісного привітання
- **Job Queue**: Використовує APScheduler з pytz для точного часового поясу

## Logging System
- **Log Channel**: -1002863334815
- **What's logged**: Всі адміністративні дії, призначення ролей, бани, мути, онлайн-режими
- **Format**: Структуровані повідомлення з іменами, username, ID та хештегами

## Configuration Management
- **Group Settings** (в config.json - тільки для власника):
  - `/adminchat [ID]` - встановити ID адмін-чату
  - `/userchat [ID]` - встановити ID чату користувачів
  - `/logchannel [ID]` - встановити ID каналу логування
  - `/testchannel [ID]` - встановити ID тестового каналу
- **Bot Control**: 
  - `/restart` - перезапустити бота (тільки власник)
  - `/deltimer [1-60]` - встановити таймер видалення відповідей ( 1-60 секунд, за замовчуванням 5)
- **Custom Names**: `/myname [ім'я]` - встановлює кастомне імʼя, яке зберігається в БД та видиме скрізь
- **Environment Variables**: BOT_TOKEN зберігається як секрет
- **Config File**: config.json для ID чатів, власників та налаштувань

## User Management
- **Info Command**: `/hto` - детальна інформація про користувача
- **Tracking**: Дата входу, виходу, перше повідомлення, хто запросив
- **Say Blocking**: `/sayb` блокувати, `/sayu` розблокувати використання say команд

## Special Features
- **Alarm System**: `/alarm` для виклику адміністрації з закріпленням
- **Broadcast**: `/broadcast` для масової розсилки
- **Online List**: `/online_list` для перегляду активних онлайн-режимів
- **Test Channel**: `/ping2` для тихого збереження в тестовий канал
- **Owner Commands**: Власник може використовувати всі команди в діалозі з ботом

# External Dependencies

## Telegram Bot API
- **Library**: python-telegram-bot (latest)
- **Purpose**: Взаємодія з Telegram API
- **Features Used**: Commands, message handlers, chat member management, permissions

## Database
- **Type**: SQLite3 (вбудована бібліотека Python)
- **File**: bot_database.db
- **Management**: database.py клас для всіх операцій

## Configuration
- **File**: config.json
- **Environment**: BOT_TOKEN з environment secrets
- **Format**: JSON з налаштуваннями чатів та ID власників

## Logging
- **System**: Python logging module
- **Level**: INFO
- **Output**: Console + канал логування

## Channels Integration
- **Log Channel**: -1002863334815 (логування дій)
- **Notes Channel**: -1002477496414 (зберігання нотаток)
- **Test Channel**: Налаштовується через /testchannel

# Commands List

**Для ВСІХ користувачів:**

👤 **ПЕРСОНАЛЬНІ НАЛАШТУВАННЯ:**
- /myname - встановити кастомне імʼя (видиме скрізь) - видалення: `-` або `clear`
- /mym - встановити профіль-гіфку/фото (reply на медіа) - видалення: `-`
- /mymt - встановити опис профілю (до 300 символів) - видалення: `-` або `clear`
- /hto - переглянути свій профіль (без аргумента)

📝 **НОТАТКИ:**
- /note - зберегти нотатку (тільки свої)
- /notes - показати свої нотатки
- /delnote - видалити нотатку за номером (тільки свої)

**Для всіх адмінів (Gnomes+):**
- /say, /says - пересилання повідомлень
- /sayon, /sayson - онлайн-режими
- /reminder - нагадування

**Для Gnomes (Гномів):**
+ Всі команди для всіх користувачів
- /ban_s, /unban_s - тихе бану (тільки reply)
- /mute_s, /unmute_s - тихе мютння (тільки reply)
- /save_s - зберегти в адмін-чат
- /hto @username або ID - переглянути профіль іншого (в адмін-чаті)
- /admin_list - список всіх адміністраторів
- /online_list - список адмінів онлайн (sayon/sayson)
- /helpg - команди для гномів (справка)
- /role_cmd - список персональних команд

**Для Head Admins:**
+ Всі команди гномів
- /ban_t, /unban_t - публічне бану + повідомлення користувачу (reply)
- /mute_t, /unmute_t - публічне мютння + повідомлення користувачу (reply)
- /kick - вигнати з чату (reply)
- /add_gnome, /remove_gnome - управління гномами
- /saypin - відправити і закріпити
- /sayb, /sayu - блокування say команд
- /alarm - виклик адміністрації
- /broadcast - розсилка
- /reminde - нагадування користувачу
- /birthdays, /addb, /setbgif, /setbtext, /previewb - дні народження
- /helpm - команди для головних адмінів (справка)

👥 **КОРИСТУВАЦЬКІ КОМАНДИ:**
- /set_cmd <назва> <команда> - створити текстовий дублер команди (Приклад: /set_cmd бан /ban)
- /del_cmd <назва> - видалити текстовий дублер команди
- /set_personal <назва> <шаблон> - створити персональну команду з плейсхолдерами
  - @s1 = відправник команди
  - @s2 = одержувач (користувач на reply)
  - @t = додатковий текст після команди
  - Приклад: /set_personal поцілувати @s1 поцілював @s2 @t
- /set_cmdm <назва> - додати медіа (фото/гіф/відео) до персональної команди (reply на медіа)
- /del_personal <назва> - видалити персональну команду
- /role_cmd або /personal - показати список всіх персональних команд

**Для Owner (Власника):**
+ Всі команди гномів та head admins
- /htop - призначити кастомну посаду користувачу (reply) - видалення: `-` або `clear`
- /add_main_admin, /remove_main_admin - управління адмінами
- /nah - чорний список
- /adminchat, /userchat, /logchannel, /testchannel - налаштування чатів та каналів
- /deltimer [1-60] - встановити таймер видалення відповідей бота (1-60 секунд)
- /restart - перезапустити бота
- /ping2 - зберегти в тестовий канал
- /notes [ID] - нотатки іншого користувача
- /hto @username або ID - переглянути профіль будь-кого (напряму в боті, повідомлення в основний чат)
- Всі команди можуть бути використані в діалозі з ботом (DM)

# Recent Changes (November 24, 2025)

**Features Implemented:**

1. **Birthday Automation & Pinning:**
   - ✅ Автоматичні привітання на дні народження о 08:00 AM Київський час
   - ✅ Інтегровано job_queue з python-telegram-bot[job-queue]
   - ✅ Система тегування: "Давайте привітаємо @username" (або ім'я)
   - ✅ Закріплення привітань в чаті для видимості
   - ✅ Модифіковано `/previewb` для чистого перегляду з закріпленням
   - ✅ GIF анімація + текст + персональний тег

2. **Note Management:**
   - ✅ Додана команда `/delnote [номер]` для видалення нотаток
   - ✅ Можна видаляти нотатки за їх номером з `/notes`
   - ✅ Логування всіх видалень у консоль
   - ✅ Перевірка наявності нотатки перед видаленням

3. **Bot Management & Help System:**
   - ✅ Додана команда `/restart` для перезапуску бота (тільки власник)
   - ✅ Оновлено всі help команди (`/help`, `/helpg`, `/helpm`, `/allcmd`)
   - ✅ Додано команди `/delnote` та `/restart` в відповідні help меню
   - ✅ Ієрархічна структура допомоги за ролями користувачів

4. **Custom Display Names:**
   - ✅ Додана таблиця `custom_names` в БД для зберігання кастомних імен
   - ✅ Команда `/myname [ім'я]` для встановлення кастомного імʼя
   - ✅ Команда `/myname clear` для видалення кастомного імʼя
   - ✅ Функція `get_display_name()` для отримання імʼя з пріоритетом: кастомне > стандартне
   - ✅ Обмеження: максимум 100 символів на імʼя
   - ✅ Система логування встановлення і видалення імен
   - ✅ Додано до всіх help меню для всіх ролей

5. **Open Access to Notes (Democratization):**
   - ✅ Видалено `can_use_bot()` перевірку для `/note` - тепер доступна ДЛЯ ВСІХ
   - ✅ Видалено `can_use_bot()` перевірку для `/notes` - тепер доступна ДЛЯ ВСІХ
   - ✅ Видалено `can_use_bot()` перевірку для `/delnote` - тепер доступна ДЛЯ ВСІХ
   - ✅ Система автоматично ізолює нотатки: користувач видит тільки свої
   - ✅ Власник може переглядати нотатки інших користувачів за `/notes [ID]`
   - ✅ Підтвердження функціональності: видалення, зберігання, отримання нотаток
   - ✅ Оновлено все help меню - позначено що команди доступні для всіх
   - ✅ **ВИПРАВЛЕНО**: Система нотаток тепер показує кастомне імʼя того хто зберіг нотатку
     - Додана колона `created_by_id` в таблицю notes для зберігання ID автора
     - При показі нотаток використовується `get_display_name()` для отримання кастомного імʼя
     - Якщо кастомне імʼя не встановлене - показується оригінальне імʼя з аккаунта

6. **Extended Profile System (Профіль-система):**
   - ✅ Додано 3 нові таблиці БД: `profile_pictures`, `profile_descriptions`, `custom_positions`
   - ✅ Команда `/mym` - встановити профіль-гіфку/фото (reply на медіа)
     - Видалення: `/mym -` 
   - ✅ Команда `/mymt` - встановити опис профілю до 300 символів
     - Видалення: `/mymt -` або `/mymt clear`
   - ✅ Команда `/myname` - встановити кастомне імʼя
     - Видалення: `/myname -` або `/myname clear`
   - ✅ Команда `/htop` - власник призначає кастомну посаду користувачу (reply ІЛИ @username/ID)
     - **Способи використання:**
       - Reply: Reply на mensaje користувача + `/htop [посада]`
       - По @username: `/htop @username [посада]`
       - По ID: `/htop 123456 [посада]`
     - Видалення: `/htop - ` або `/htop clear` (reply)
   - ✅ Розширена команда `/hto`:
     - **Для звичайних користувачів:**
       - Без аргументів - показує інформацію про себе
       - З ID/username - ❌ заборонено (тільки для адмінів)
     - **Для гномів, головних адмінів та власника (в АДМІН-ЧАТІ):**
       - Без аргументів - показує інформацію про себе
       - З ID або @username - показує інформацію про будь-кого (`/hto @username` або `/hto 123456`)
     - **Для власника (напряму в боті, DM):**
       - Без аргументів - показує інформацію про себе
       - З ID або @username - показує інформацію про будь-кого, повідомлення відправляється в основний чат
     - **Формат виводу:**
       - Профіль-гіфку/фото надсилається ПЕРШОЮ з текстом як описп (як привітання з днем народження)
       - Якщо немає фото - показується просто текст
       - Виводить: кастомне імʼя, опис, @username, ID, дату вступу, посаду
       - Правильно визначає роль: 👑 Власник, 🔒 Головний Адмін, 🧙 Гном, 👤 Користувач
   - ✅ Оригінальне імʼя аккаунта логується скрізь (користувач [ID] {full_name} {username})
   - ✅ Всі команди логуються в LOG_CHANNEL
   - ✅ Кастомна посада не впливає на права (тільки видиме)
   - ✅ При видаленні всі налаштування повертаються до стандартних значень

7. **Moderation Commands (Покращення):**
   - ✅ **BAN система:**
     - `/ban_s` - тихий бан (reply на mensaje) - РЕАЛЬНИЙ БАНУ, без публічного повідомлення
     - `/ban_t` - публічний бан (reply на mensaje) - РЕАЛЬНИЙ БАНУ + повідомлення в чат + DM користувачу
     - `/unban_s`, `/unban_t` - розбан з логуванням
   - ✅ **MUTE система:**
     - `/mute_s` - тихий мут (reply на mensaje) - РЕАЛЬНИЙ МУТ, без публічного повідомлення  
     - `/mute_t` - публічний мут (reply на mensaje) - РЕАЛЬНИЙ МУТ + повідомлення в чат
     - `/unmute_s`, `/unmute_t` - розмут з логуванням
   - ✅ **KICK система:**
     - `/kick` - вигнання з чату (reply на mensaje) - РЕАЛЬНИЙ KICK via ban+unban
   - ✅ **Права доступу:**
     - Гноми можуть виконувати `/ban_s`, `/mute_s`, `/kick` (reply)
     - Head Admins можуть виконувати всі команди (reply, крім ban_t/mute_t) + `/ban_t`, `/mute_t`
     - Owner може виконувати всі команди всюди (адмін-чат та DM)
   - ✅ Все логується в LOG_CHANNEL з інформацією про адміна та користувача
   - ✅ Бану та мути перевіряються при вході користувача (блокування доступу)

8. **Auto-Delete Responses (Покращення UX):**
   - ✅ Всі відповіді бота про успіх/помилку видаляються через встановлений таймер
   - ✅ Зменшує спам в чаті та покращує UX
   - ✅ Використовується для всіх команд (206+ reply_text)
   - ✅ Асинхронне видалення не блокує бот
   - ✅ Логування в канали НЕ видаляється (залишається для історії)
   - ✅ Публічні повідомлення в основний чат НЕ видаляються (залишаються для видимості)
   - ✅ **Команда /deltimer [1-60]** - власник може коригувати таймер видалення
     - За замовчуванням: 5 секунд
     - Мінімум: 1 секунда
     - Максимум: 60 секунд
     - Значення зберігається в config.json для персистентності 

# Recent Changes (December 27, 2025)

**System Updates & Help Command Refactoring:**

1. **Owner ID Restriction:**
   - ✅ Обмежено доступ власника до 2 користувачів: 7247114478 та 7516733683
   - ✅ Видалено інших користувачів з OWNER_IDS в config.json
   - ✅ Тільки ці 2 ID можуть використовувати команди налаштування групи та адмін-команди

2. **Help Commands Refactoring:**
   - ✅ `/helpg` - переписано для гномів з правильною організацією команд по категоріях
   - ✅ `/helpm` - переписано для головних адмінів з повним списком дозволених команд
   - ✅ `/allcmd` - переписано для власника з усіма 100+ командами, організованими по категоріях
   - ✅ Всі допоміжні команди містять чіткі посилання на інші справки

3. **Configuration Verification:**
   - ✅ Group settings вже були в config.json (ADMIN_CHAT_ID, USER_CHAT_ID, LOG_CHANNEL_ID, TEST_CHANNEL_ID)
   - ✅ Контролюються командами `/adminchat`, `/userchat`, `/logchannel`, `/testchannel`
   - ✅ Зберігаються автоматично при зміні

# Recent Changes (November 25, 2025)

**Advanced Moderation Features:**

1. **Time-Based Auto-Unmute System:**
   - ✅ `/mute_t` команда тепер підтримує часові параметри: `30s`, `1m`, `5m`, `1h`, `2h`
   - ✅ Формат: `/mute_t [час] [причина]` (обидва параметри необов'язкові)
   - ✅ Автоматичний розмут через вказаний час - бот сам розмутить користувача! ⏱️
   - ✅ Асинхронна обробка з логуванням в консоль
   - ✅ Київський часовий пояс (pytz) для точного розрахунку часу розмута

2. **Unified Moderation Message Format:**
   - ✅ Всі адміністративні команди (`/ban_t`, `/mute_t`, `/unmute_t`, `/unban_t`, `/kick`) мають єдиний формат
   - ✅ **Динамічні повідомлення**: Причина не показується, якщо не вказана
   - ✅ **Формат**: `[Емодзі] [Користувач] [Дія]. До: [Час/∞]. [Причина: якщо є]. Адмін: [Клікабельне імʼя]`
   - ✅ **Клікабельні імена адмінів**: HTML посилання на профіль для всіх адміністративних дій
   - ✅ Все логується в LOG_CHANNEL з інформацією про адміна

3. **Updated Help Commands:**
   - ✅ `/help` - базова допомога для користувачів
   - ✅ `/helpg` - допомога для гномів з новим розділом МОДЕРАЦІЯ
   - ✅ `/helpm` - допомога для головних адмінів з описанням нових функцій `/mute_t`
   - ✅ `/allcmd` - повний список всіх команд власника з деталями про часові формати

4. **Documentation Updates:**
   - ✅ `BOT_DOCUMENTATION.txt` - повна документація з новими форматами команд
   - ✅ `replit.md` - архітектурна документація з деталями нової модерації системи
   - ✅ Описані всі часові формати та автоматичні функції

# Previous Production Release (October 23, 2025)

**Production-Ready Release:**
- ✅ Повна переробка бота з JSON на SQLite (1833 рядки коду)
- ✅ Додано 30+ команд з повною підтримкою ID, @username та reply
- ✅ Створено систему ролей з 2 власниками (7247114478, 7516733683)
- ✅ Додано логування всіх дій в канал -1002863334815
- ✅ Реалізовано онлайн-режими пересилання (sayon/sayson)
- ✅ Додано систему днів народження з кастомними привітаннями
- ✅ Додано нотатки та нагадування з інтеграцією каналу -1002477496414
- ✅ Реалізовано чорний список (тільки Owner)
- ✅ Додано динамічні налаштування чатів через команди
- ✅ Власник може використовувати всі команди в діалозі з ботом
- ✅ Виправлено всі LSP помилки
- ✅ Пройдено архітектурне ревʼю - готово до production
- ✅ Видалено всі застарілі файли
